diff --git a/src/qtgui/plotter.cpp b/src/qtgui/plotter.cpp
index 758b075..6495196 100644
--- a/gqrx/src/qtgui/plotter.cpp
+++ b/gqrx/src/qtgui/plotter.cpp
@@ -1041,7 +1041,11 @@ void CPlotter::wheelEvent(QWheelEvent * event)
     }
     else if (m_CursorCaptured == XAXIS)
     {
-        zoomStepX(delta < 0 ? 1.1 : 0.9, px);
+        // Scroll faster when Ctrl is held
+        float stepSize = event->modifiers() & Qt::ControlModifier ? 0.3 : 0.1;
+        // Zoom out or in
+        float step = 1 + (delta < 0 ? stepSize : -stepSize);
+        zoomStepX(step, px);
     }
     else if (event->modifiers() & Qt::ControlModifier)
     {
